//creating the screen component for the Audio seach with the speech to text feature.
// need to install the react-native-voice package

import { View, Text } from 'react-native'
import React from 'react'

export default function AudioSearch() {
  return (
    <View>
      <Text>AudioSearch</Text>
    </View>
  )
}